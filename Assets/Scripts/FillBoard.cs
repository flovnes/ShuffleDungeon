using UnityEngine;

public class FillBoard : MonoBehaviour {
    // called once before the first execution of Update() after MonoBehaviour is created
    void Start() {
        GenerateBoard();
    }

    // called once per frame
    void Update() {
        
    }
}

void GenerateBoard() {
    // ^
}
